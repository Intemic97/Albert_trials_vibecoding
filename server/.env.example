# =============================================
# Environment Variables - Backend
# Copy to server/.env and fill with your real values
# =============================================
#
# SECURITY NOTICE:
# - NEVER commit .env files to version control
# - Rotate ALL secrets if they have ever been exposed
# - Use a secrets manager (AWS SM, Vault) in production
# - JWT_SECRET must be a strong random string (min 32 chars)
#

# --- Authentication (REQUIRED) ---
JWT_SECRET=generate-a-strong-random-string-min-32-chars
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# --- LLM / AI (copilots, workflows) ---
OPENAI_API_KEY=sk-your-openai-key-here

# --- Email (verification, notifications) ---
RESEND_API_KEY=re_your_resend_key_here
RESEND_FROM=YourApp <onboarding@resend.dev>

# --- URLs ---
APP_URL=https://your-domain.com
PREFECT_SERVICE_URL=http://localhost:8000
NODE_ENV=production

# --- Prefect Cloud (optional) ---
PREFECT_API_KEY=your_prefect_api_key
PREFECT_API_URL=https://api.prefect.cloud/api/accounts/xxx/workspaces/yyy

# --- Stripe (payments) ---
STRIPE_PRICE_PRO=price_xxx
STRIPE_PRICE_BUSINESS=price_xxx
STRIPE_SECRET_KEY=sk_live_xxx

# --- AWS Lambda (remote Python execution) ---
AWS_REGION=eu-north-1
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
LAMBDA_FUNCTION_NAME=python-executor
USE_LAMBDA_FOR_PYTHON=true

# --- Google (Sheets, GCS) ---
GOOGLE_API_KEY=AIzaSy-your-google-api-key
GCP_PROJECT_ID=your-project
GCS_BUCKET_NAME=your-bucket
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json

# --- Google OAuth ---
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# --- Climatiq (optional) ---
CLIMATIQ_API_KEY=your_climatiq_api_key

# --- CORS (comma-separated allowed origins) ---
CORS_ORIGINS=http://localhost:5173,https://your-domain.com
