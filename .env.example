# =============================================
# Variables de entorno - Build frontend (Vite)
# Copia a .env en la raíz del proyecto
# =============================================

# Base URL del API (usada en build del frontend)
# Por defecto: /api (funciona si nginx/proxy redirige /api al backend)
# Solo cambia si el API está en otro dominio:
# VITE_API_BASE=https://api.tu-dominio.com
# VITE_API_BASE=/api
